# Core
pandas>=2.1.0
numpy>=1.26.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
PyYAML>=6.0.1

# Scheduling
APScheduler>=3.10.4

# Data / analysis
scipy>=1.12.0

# Email
# (smtplib is stdlib — no extra package needed)

# Development / testing
pytest>=8.0.0
pytest-cov>=5.0.0
faker>=24.0.0       # generate test data

# Optional: Power BI REST API refresh
requests>=2.31.0
msal>=1.26.0
